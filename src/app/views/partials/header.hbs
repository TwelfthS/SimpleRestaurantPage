<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: rgb(190, 0, 0)">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link {{home}}" aria-current="page" href="/">Главная</a>
        {{#unless session.isAdmin}}<a class="nav-link {{orders}}" href="/orders">Заказы</a>{{/unless}}
        <a class="nav-link {{menu}}" href="/menu">Меню</a>
      </div>
      

      <div class="navbar-nav ms-auto">
        {{#if session.userId}}
        <a href="/logout" class="nav-link">Выйти</a>
        {{else}}
        <a href="/signin" class="nav-link">Войти</a>
        <a href="/signup" class="nav-link">Регистрация</a>
        {{/if}}
      </div>
    </div>
  </div>
</nav>